import React from "react";
import ReactDOM from "react-dom/client";
import StressForm from "./StressForm";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<StressForm />);
